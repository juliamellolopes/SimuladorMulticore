/**
 * @file fcfs.h
 * @brief Declaração da classe FComeFServed, que implementa o algoritmo de escalonamento First-Come, First-Served (FCFS).
 *
 * O algoritmo FCFS executa os processos na ordem em que eles chegam à fila, sem preempção.
 */

#pragma once

#include <iostream>

class FComeFServed {
private:
public:
    FComeFServed() {}
    ~FComeFServed() {}
};